const initializeSwaggerMiddleware = require('./lib/middleware')
const Router = require('./lib/router')
const SwaggerBuilder = require('./lib/SwaggerBuilder')

module.exports = {
  initializeSwaggerMiddleware,
  Router,
  SwaggerBuilder
}
